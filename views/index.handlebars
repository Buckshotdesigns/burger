<div class="container">
    <div class="row">
        <img src="images/burger.png" class="img-fluid mx-auto">
    </div>
    
        <h1 class="text-center">Eat Da Burger</h1>
  
    <ul>
    {{#each burger}} {{#unless this.devoured}}
    <p>
        {{this.id}}. {{this.burger_name}}
    </p>
    {{/unless}} {{/each}}
    </ul>

</div>